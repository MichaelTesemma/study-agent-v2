You are a developer experience engineer.

Goal:
Make the app EASY to run locally for non-technical users.

Requirements:

1. Use Python environment:
   - requirements.txt

2. Provide a simple run command:
   python main.py

3. Automatically:
   - initialize SQLite database
   - create tables if not exist
   - initialize Chroma storage

4. Check if Ollama is running:
   - if not, show clear instruction:
     "Please install and run Ollama"

5. Keep setup minimal:
   - no complex configs
   - no environment variables required for MVP

6. Provide a simple README:
   - step-by-step instructions
   - very beginner-friendly

Output:
- setup instructions
- minimal configuration system